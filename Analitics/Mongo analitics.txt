1. Nell'arco degli ultimi 5 anni, trovare per ogni azienda quale sia stato il mese dell'anno più redditizio secondo il valore di chiusura
2. Per ogni titolo verificare se nell'ultima settimana l'interesse è cresiuto o diminuito, ovvero se per ogni giorno il volume è maggiore del volume medio (le aziende con volume medio inferiore a.....)
3. Per ogni settore, trovare le aziende con capitalizzazione superiore ad un miliardo che hanno il rapporto PE più basso (le aziende più sottovalutate)

{ $month:{ $dateFromString: { dateString: "06-15-2018", format: "%m-%d-%Y" } } }


db.history.aggregate( 
  { $match: { Symbol:"AAPL"}},
  { $project: { symbol: "$Symbol", date: { $month:{ $dateFromString: { dateString: '$Date', format: "%Y-%m-%d" } } } } }
    )
db.history.aggregate( 
  { $match: { Symbol:"AAPL"}},
  { $project: { symbol: "$Symbol", month: { $month:{ $dateFromString: { dateString: '$Date', format: "%Y-%m-%d" } } }, day: { $dayOfMonth: { $dateFromString: { dateString: '$Date', format: "%Y-%m-%d" } } } } }
    )

